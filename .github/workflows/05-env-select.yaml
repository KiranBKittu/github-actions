name: Environment
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Test
        type: choice
        required: true
        options:
          - DEV
          - PROD
jobs:
  deploy:
    steps:
      - name: check status
        run: echo "Just for check in ${{ github.event.inputs.environment }}"
