name: Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Pull latest code on AWS
        run: |
          cd ~/app/self-hosted-01
          git pull origin main
